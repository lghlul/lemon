<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dao.TeacherClazzDao">


    <insert id="save" parameterType="com.demo.model.TeacherClazz" keyProperty="teacherClazzId" useGeneratedKeys="true">
    insert into teacher_clazz(teacherNumber , clazzNumber  , createTime ) values( #{teacherNumber} , #{clazzNumber}  ,now())
   </insert>
    <update id="update" parameterType="com.demo.model.TeacherClazz">
        update teacher_clazz
        set
            term = #{term}
        where teacherNumber = #{teacherNumber} and clazzNumber = #{clazzNumber}
    </update>


    <select id="list" parameterType="com.demo.dto.TeacherClazzQuery" resultType="com.demo.model.TeacherClazz">
      select * from teacher_clazz
      <where>
        <if test="teacherNumber != null">
          teacherNumber = #{teacherNumber}
        </if>
      </where>
    </select>

    <select id="listClazz" parameterType="int" resultType="com.demo.model.ClazzTermReport">
        SELECT
        b.term,
        max(score) maxScore,
        min(score) minScore,
        AVG(score) avgScore,
        a.clazzNumber
        FROM
        teacher_clazz a
        inner JOIN student_clazz b ON a.clazzNumber = b.clazzNumber
        <where>
            <if test="teacherNumber != null">
                and teacherNumber = #{teacherNumber}
            </if>
        </where>
        GROUP BY
        term,
        clazzNumber
    </select>


    <select id="listTeacherByTeacherLevel" resultType="com.demo.model.TeacherClazzReport">
        SELECT
            a.teacherNumber,
            max(score) maxScore,
            min(score) minScore,
            AVG(score) avgScore,
            a.clazzNumber
        FROM
            teacher_clazz a
        inner JOIN student_clazz b ON a.clazzNumber = b.clazzNumber
        GROUP BY
            teacherNumber,
            clazzNumber
    </select>

    <delete id="delete" parameterType="int">
      delete from teacher_clazz where teacherNumber = #{teacherNumber} and clazzNumber = #{clazzNumber}
    </delete>


</mapper>
