<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dao.TeacherDao">


    <insert id="save" parameterType="teacherDTO" keyProperty="teacherId" useGeneratedKeys="true">
    insert into teacher(teacherNumber , teacherName , teacherLevel , createTime) values(#{teacherNumber} , #{teacherName} , #{teacherLevel} , #{createTime})
   </insert>

    <select id="get" parameterType="teacherDTO" resultType="teacher">
        select * from teacher
        <where>
            <if test="teacherId != null">
                and teacherId = #{teacherId}
            </if>
            <if test="teacherNumber != null">
                and teacherNumber = #{teacherNumber}
            </if>
        </where>
    </select>

    <select id="list" parameterType="listTeacherDTO" resultType="teacher">
        select * from teacher
        <where>
            <if test="teacherName != null">
                and teacherName like CONCAT('%',#{teacherName},'%')
            </if>
            <if test="teacherLevel != null">
                and teacherLevel = #{teacherLevel}
            </if>
        </where>
    </select>


    <update id="update" parameterType="teacherDTO">
        update teacher
        <set>
            <if test="teacherName != null">
                teacherName = #{teacherName},
            </if>
            <if test="teacherLevel != null">
                teacherLevel = #{teacherLevel},
            </if>
            <if test="teacherNumber != null">
                teacherNumber = #{teacherNumber},
            </if>
        </set>
        where teacherId = #{teacherId}
    </update>

    <delete id="delete" parameterType="int">
      delete from teacher where teacherId = #{teacherId}
    </delete>

</mapper>
